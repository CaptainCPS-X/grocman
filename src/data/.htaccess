# Bloquea el acceso web directo a la base de datos.
# PHP la sigue leyendo por el sistema de archivos; solo se niega el acceso por URL.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
